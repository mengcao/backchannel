<%= form_for ([@parent,@vote]) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :votable_id %><br />
    <%= f.number_field :votable_id %>
  </div>
  <div class="field">
    <%= f.label :votable_type %><br />
    <%= f.text_field :votable_type %>
  </div>
  <div class="field">
    <%= f.label :user %><br/>
    <%= select("vote","user_id",User.find(:all).collect {|u| [u.name,u.id]})%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
